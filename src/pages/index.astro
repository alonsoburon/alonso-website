---
import { getCollection } from 'astro:content';
import Layout from '../layouts/Layout.astro';

const posts = await getCollection('blog');
const recentPosts = posts.slice(0, 3);

function formatDate(dateString: string) {
  const date = new Date(dateString);
  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric'
  });
}
---

<Layout title="Alonso - Ingeniero de Datos" description="CV y portafolio de Alonso, especialista en ingeniería de datos, machine learning y análisis de datos.">
  <div class="container">
    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-content">
        <h1>Jefe de Célula - Data Team Lead</h1>
        <p class="hero-subtitle">Creando el mejor extractor de datos en LATAM</p>
        <p class="hero-description">
          Liderando la primera Célula de Ingeniería de Datos en Datawalt. 
          Tech nerd con pasión por la automatización y eficiencia. 
          Estudiando Data Science mientras trabajo full-time como ingeniero de BI y datos.
        </p>
        <div class="hero-buttons">
          <a href="/proyectos" class="btn btn-primary">Ver Proyectos</a>
          <a href="/contacto" class="btn btn-secondary">Contactar</a>
        </div>
      </div>
    </section>

    <!-- Interactive Data Pipeline -->
    <section class="pipeline-demo">
      <h2>Live Data Pipeline</h2>
      <div class="pipeline-container">
        <div class="pipeline-node" data-node="source" onclick="showNodeDetails('source')">
          <div class="node-header">
            <div class="node-status status-success"></div>
            <div class="node-icon">📊</div>
          </div>
          <div class="node-content">
            <h3>Fuentes de Datos</h3>
            <p>APIs • Bases de Datos • Archivos</p>
            <div class="node-metrics">
              <span class="metric">Múltiples fuentes</span>
              <span class="metric">Validación en tiempo real</span>
            </div>
          </div>
        </div>

        <div class="pipeline-connection">
          <div class="connection-line"></div>
          <div class="data-flow"></div>
        </div>

        <div class="pipeline-node" data-node="etl" onclick="showNodeDetails('etl')">
          <div class="node-header">
            <div class="node-status status-success"></div>
            <div class="node-icon">⚡</div>
          </div>
          <div class="node-content">
            <h3>Motor ETL</h3>
            <p>Python • Power BI • Automatización</p>
            <div class="node-metrics">
              <span class="metric">Software propio</span>
              <span class="metric">40% más eficiente</span>
            </div>
          </div>
        </div>

        <div class="pipeline-connection">
          <div class="connection-line"></div>
          <div class="data-flow"></div>
        </div>

        <div class="pipeline-node" data-node="warehouse" onclick="showNodeDetails('warehouse')">
          <div class="node-header">
            <div class="node-status status-success"></div>
            <div class="node-icon">🗄️</div>
          </div>
          <div class="node-content">
            <h3>Data Warehouse</h3>
            <p>PostgreSQL • Arquitectura Unificada</p>
            <div class="node-metrics">
              <span class="metric">Escalable</span>
              <span class="metric">Alta disponibilidad</span>
            </div>
          </div>
        </div>

        <div class="pipeline-connection">
          <div class="connection-line"></div>
          <div class="data-flow"></div>
        </div>

        <div class="pipeline-node" data-node="analytics" onclick="showNodeDetails('analytics')">
          <div class="node-header">
            <div class="node-status status-success"></div>
            <div class="node-icon">📈</div>
          </div>
          <div class="node-content">
            <h3>Capa Analítica</h3>
            <p>Power BI • Dashboards • KPIs</p>
            <div class="node-metrics">
              <span class="metric">Múltiples dashboards</span>
              <span class="metric">Reportes automatizados</span>
            </div>
          </div>
        </div>
      </div>

      <div class="pipeline-details" id="nodeDetails">
        <div class="details-content">
          <h3 id="detailsTitle">Haz clic en un nodo para ver detalles</h3>
          <p id="detailsDescription">Selecciona cualquier componente del pipeline para ver su configuración y métricas de rendimiento.</p>
        </div>
      </div>
    </section>

    <!-- Experience Section -->
    <section class="experience">
      <h2>Experiencia Profesional</h2>
      <div class="timeline">
        <div class="timeline-item">
          <div class="timeline-marker"></div>
          <div class="timeline-content">
            <h3>Data Team Lead</h3>
            <p class="company">Datawalt</p>
            <p class="period">Mayo 2025 - Presente</p>
            <ul>
              <li>Lideré la creación de software para unificar y escalar la gestión de información corporativa</li>
              <li>Fundé y lidero la primera Célula de Ingeniería de Datos de la compañía</li>
              <li>Dirijo un equipo enfocado en mejorar integraciones de software y crear nuevas</li>
              <li>Objetivo: crear el mejor extractor de datos en LATAM</li>
            </ul>
          </div>
        </div>
        <div class="timeline-item">
          <div class="timeline-marker"></div>
          <div class="timeline-content">
            <h3>Ingeniero de Desarrollo BI</h3>
            <p class="company">Datawalt</p>
            <p class="period">Mayo 2024 - Mayo 2025</p>
            <ul>
              <li>Desarrollé soluciones de Business Intelligence escalables</li>
              <li>Gestioné proyectos y clientes con enfoque en resultados</li>
              <li>Combiné habilidades técnicas y de gestión para el crecimiento</li>
            </ul>
          </div>
        </div>
        <div class="timeline-item">
          <div class="timeline-marker"></div>
          <div class="timeline-content">
            <h3>Data Science Student</h3>
            <p class="company">IU International University of Applied Sciences</p>
            <p class="period">Marzo 2024 - Febrero 2027</p>
            <ul>
              <li>Estudiando Bachelor of Applied Science en Data Science</li>
              <li>Enfoque en machine learning, estadística y análisis de datos</li>
              <li>Desarrollando proyectos prácticos en Python y herramientas de BI</li>
            </ul>
          </div>
        </div>
        <div class="timeline-item">
          <div class="timeline-marker"></div>
          <div class="timeline-content">
            <h3>Associate's Degree</h3>
            <p class="company">Pontificia Universidad Católica de Chile</p>
            <p class="period">Septiembre 2023 - Junio 2024</p>
            <ul>
              <li>Completé Associate's degree en análisis de datos</li>
              <li>Especialización en herramientas de visualización y BI</li>
              <li>Proyectos finales en Python y SQL</li>
            </ul>
          </div>
        </div>
        <div class="timeline-item">
          <div class="timeline-marker"></div>
          <div class="timeline-content">
            <h3>Stata and Python Private Tutor</h3>
            <p class="company">Autónomo</p>
            <p class="period">Agosto 2021 - Enero 2024</p>
            <ul>
              <li>Tutorías privadas de análisis de datos usando Stata y Python</li>
              <li>Enseñé a estudiantes de la FEN (Facultad de Economía y Negocios)</li>
              <li>Desarrollé habilidades de comunicación y enseñanza</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- Recent Posts -->
    <section class="recent-posts">
      <h2>Latest Articles</h2>
      <div class="posts-grid">
        {recentPosts.map((post) => (
          <article class="post-card">
            <h3><a href={`/blog/${post.slug}`}>{post.data.title}</a></h3>
            <p class="post-meta">{formatDate(post.data.date)}</p>
            <p>{post.data.description || 'Article about data engineering and analysis.'}</p>
          </article>
        ))}
      </div>
      <div class="text-center">
        <a href="/blog" class="btn btn-outline">View All Articles</a>
      </div>
    </section>
  </div>

  <style>
    /* Hero Section */
    .hero {
      text-align: center;
      padding: 4rem 0;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      margin: -2rem 0 4rem 0;
      border-radius: 0 0 0 0;
    }

    .hero-content {
      max-width: 800px;
      margin: 0 auto;
      padding: 0 1rem;
    }

    .hero h1 {
      font-size: 3.5rem;
      font-weight: 700;
      margin-bottom: 1rem;
    }

    .hero-subtitle {
      font-size: 1.5rem;
      font-weight: 300;
      margin-bottom: 1rem;
      opacity: 0.9;
    }

    .hero-description {
      font-size: 1.1rem;
      margin-bottom: 2rem;
      opacity: 0.8;
      line-height: 1.7;
    }

    .hero-buttons {
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
    }

    /* Buttons */
    .btn {
      display: inline-block;
      padding: 0.75rem 1.5rem;
      border-radius: 0.5rem;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s;
      border: 2px solid transparent;
    }

    .btn-primary {
      background: white;
      color: #667eea;
    }

    .btn-primary:hover {
      background: #f8f9fa;
      transform: translateY(-2px);
    }

    .btn-secondary {
      background: transparent;
      color: white;
      border-color: white;
    }

    .btn-secondary:hover {
      background: white;
      color: #667eea;
    }

    .btn-outline {
      background: transparent;
      color: #2563eb;
      border-color: #2563eb;
    }

    .btn-outline:hover {
      background: #2563eb;
      color: white;
    }

    /* Sections */
    section {
      margin-bottom: 4rem;
    }

    h2 {
      font-size: 2.5rem;
      font-weight: 700;
      text-align: center;
      margin-bottom: 3rem;
      color: #1f2937;
    }

    /* Interactive Pipeline Section */
    .pipeline-demo {
      padding: 4rem 0;
      background: #0f172a;
      color: white;
    }

    .pipeline-demo h2 {
      text-align: center;
      font-size: 2.5rem;
      font-weight: 700;
      color: #f1f5f9;
      margin-bottom: 3rem;
    }

    .pipeline-container {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      max-width: 1400px;
      margin: 0 auto 3rem;
      padding: 0 1rem;
    }

    .pipeline-node {
      background: #1e293b;
      border: 2px solid #334155;
      border-radius: 12px;
      padding: 1.25rem;
      width: 280px;
      transition: all 0.3s ease;
      cursor: pointer;
      position: relative;
      overflow: hidden;
      flex-shrink: 0;
    }

    .pipeline-node:hover {
      border-color: #10b981;
      box-shadow: 0 0 20px rgba(16, 185, 129, 0.3);
      transform: translateY(-2px);
    }

    .pipeline-node.active {
      border-color: #10b981;
      box-shadow: 0 0 20px rgba(16, 185, 129, 0.5);
    }

    .node-header {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      margin-bottom: 1rem;
    }

    .node-status {
      width: 12px;
      height: 12px;
      border-radius: 50%;
      animation: pulse 2s infinite;
    }

    .status-success {
      background: #10b981;
      box-shadow: 0 0 10px rgba(16, 185, 129, 0.5);
    }

    .node-icon {
      font-size: 1.5rem;
    }

    .node-content h3 {
      font-size: 1.125rem;
      font-weight: 600;
      color: #f1f5f9;
      margin-bottom: 0.5rem;
    }

    .node-content p {
      color: #94a3b8;
      font-size: 0.875rem;
      margin-bottom: 1rem;
    }

    .node-metrics {
      display: flex;
      flex-direction: column;
      gap: 0.25rem;
    }

    .metric {
      background: #334155;
      color: #10b981;
      padding: 0.25rem 0.5rem;
      border-radius: 4px;
      font-size: 0.75rem;
      font-weight: 500;
      text-align: center;
    }

    .pipeline-connection {
      display: flex;
      align-items: center;
      position: relative;
      width: 30px;
      height: 4px;
      flex-shrink: 0;
    }

    .connection-line {
      width: 100%;
      height: 2px;
      background: #334155;
      position: relative;
    }

    .data-flow {
      position: absolute;
      width: 8px;
      height: 8px;
      background: #10b981;
      border-radius: 50%;
      animation: flow 3s linear infinite;
      box-shadow: 0 0 10px rgba(16, 185, 129, 0.8);
    }

    .pipeline-details {
      background: #1e293b;
      border: 1px solid #334155;
      border-radius: 12px;
      padding: 2rem;
      max-width: 800px;
      margin: 0 auto;
    }

    .details-content h3 {
      color: #f1f5f9;
      font-size: 1.5rem;
      font-weight: 600;
      margin-bottom: 1rem;
    }

    .details-content p {
      color: #94a3b8;
      line-height: 1.6;
    }

    @keyframes pulse {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.5; }
    }

    @keyframes flow {
      0% { left: -10px; }
      100% { left: 100%; }
    }

    /* Responsive */
    @media (max-width: 1200px) {
      .pipeline-container {
        flex-wrap: wrap;
        gap: 1rem;
      }
      
      .pipeline-node {
        width: calc(50% - 1rem);
        min-width: 250px;
      }
      
      .pipeline-connection {
        width: 100%;
        height: 30px;
        transform: rotate(90deg);
      }
    }

    @media (max-width: 768px) {
      .pipeline-container {
        flex-direction: column;
        gap: 1.5rem;
      }
      
      .pipeline-node {
        width: 100%;
        max-width: 300px;
        margin: 0 auto;
      }
      
      .pipeline-connection {
        transform: rotate(90deg);
        width: 4px;
        height: 40px;
      }
    }

    /* Experience Section */
    .timeline {
      position: relative;
      max-width: 800px;
      margin: 0 auto;
    }

    .timeline::before {
      content: '';
      position: absolute;
      left: 2rem;
      top: 0;
      bottom: 0;
      width: 2px;
      background: #e5e7eb;
    }

    .timeline-item {
      position: relative;
      margin-bottom: 3rem;
      padding-left: 5rem;
    }

    .timeline-marker {
      position: absolute;
      left: 1.25rem;
      top: 0.5rem;
      width: 1.5rem;
      height: 1.5rem;
      background: #2563eb;
      border-radius: 50%;
      border: 4px solid white;
      box-shadow: 0 0 0 4px #e5e7eb;
    }

    .timeline-content {
      background: white;
      padding: 2rem;
      border-radius: 1rem;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    }

    .timeline-content h3 {
      font-size: 1.5rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: #1f2937;
    }

    .company {
      font-weight: 600;
      color: #2563eb;
      margin-bottom: 0.25rem;
    }

    .period {
      color: #6b7280;
      font-size: 0.875rem;
      margin-bottom: 1rem;
    }

    .timeline-content ul {
      list-style: none;
      padding: 0;
    }

    .timeline-content li {
      position: relative;
      padding-left: 1.5rem;
      margin-bottom: 0.5rem;
    }

    .timeline-content li::before {
      content: '▸';
      position: absolute;
      left: 0;
      color: #2563eb;
      font-weight: bold;
    }

    /* Recent Posts */
    .posts-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
      gap: 2rem;
      margin-bottom: 2rem;
    }

    .post-card {
      background: white;
      padding: 2rem;
      border-radius: 1rem;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .post-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 25px rgba(0,0,0,0.15);
    }

    .post-card h3 {
      font-size: 1.25rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
    }

    .post-card h3 a {
      color: #1f2937;
      text-decoration: none;
    }

    .post-card h3 a:hover {
      color: #2563eb;
    }

    .post-meta {
      color: #6b7280;
      font-size: 0.875rem;
      margin-bottom: 1rem;
    }

    .text-center {
      text-align: center;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .hero h1 {
        font-size: 2.5rem;
      }

      .hero-subtitle {
        font-size: 1.25rem;
      }

      .hero-buttons {
        flex-direction: column;
        align-items: center;
      }

      .timeline::before {
        left: 1rem;
      }

      .timeline-item {
        padding-left: 3rem;
      }

      .timeline-marker {
        left: 0.25rem;
      }

      .posts-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>

  <script is:inline>
    const nodeDetails = {
      source: {
        title: "Fuentes de Datos",
        description: "Integré múltiples fuentes de datos corporativas para unificar la plataforma de datos de Datawalt. Desarrollé conectores personalizados para sistemas legacy y APIs REST. Implementé validación en tiempo real y controles de calidad de datos automatizados para asegurar la integridad de la información."
      },
      etl: {
        title: "Motor ETL",
        description: "Lideré la creación del software de manejo de datos homogéneo a nivel corporativo. Automatizé procesos de extracción, transformación y carga usando Python y herramientas de BI. Optimicé pipelines para mejorar la eficiencia y reducir tiempos de procesamiento en un 40%."
      },
      warehouse: {
        title: "Data Warehouse",
        description: "Diseñé y gestioné la arquitectura de datos unificada de Datawalt. Implementé soluciones de almacenamiento escalables usando PostgreSQL y sistemas de BI. Mantuve alta disponibilidad y optimicé consultas para mejorar el rendimiento de reportes y dashboards."
      },
      analytics: {
        title: "Capa Analítica",
        description: "Desarrollé dashboards interactivos en Power BI para diferentes áreas de la empresa. Creé reportes automatizados y visualizaciones que permiten tomar decisiones basadas en datos. Implementé métricas de negocio clave y KPIs para monitoreo en tiempo real."
      }
    };

    function showNodeDetails(nodeId) {
      console.log('Clicked node:', nodeId);
      
      // Remove active class from all nodes
      document.querySelectorAll('.pipeline-node').forEach(node => {
        node.classList.remove('active');
      });
      
      // Add active class to clicked node
      const clickedNode = document.querySelector(`[data-node="${nodeId}"]`);
      if (clickedNode) {
        clickedNode.classList.add('active');
        console.log('Added active class to node');
      }
      
      // Update details
      const details = nodeDetails[nodeId];
      if (details) {
        const titleElement = document.getElementById('detailsTitle');
        const descriptionElement = document.getElementById('detailsDescription');
        if (titleElement) {
          titleElement.textContent = details.title;
          console.log('Updated title:', details.title);
        }
        if (descriptionElement) {
          descriptionElement.textContent = details.description;
          console.log('Updated description');
        }
      }
    }

    // Make function globally available
    window.showNodeDetails = showNodeDetails;

    // Add some random status changes for realism
    setInterval(() => {
      const nodes = document.querySelectorAll('.pipeline-node');
      nodes.forEach(node => {
        if (Math.random() < 0.1) { // 10% chance
          const status = node.querySelector('.node-status');
          if (status) {
            status.style.animation = 'none';
            setTimeout(() => {
              status.style.animation = 'pulse 2s infinite';
            }, 100);
          }
        }
      });
    }, 5000);
  </script>
</Layout>
